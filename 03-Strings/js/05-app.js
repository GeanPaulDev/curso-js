const producto = "Monitor de 20 Pulgadas";

//Remplazar un texto por otra dentro de una cadena,sensible a mayúsculas y minúsculas
console.log(producto.replace("Pulgadas", '"'));

//Extraer un texto dentro de una cadena y muestra el resultado
console.log(producto.slice(0,10));